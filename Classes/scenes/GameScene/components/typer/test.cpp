#include <iostream>
#include <vector>
using namespace std;

int main() {
    string posWords[] = {
        "apple", "an", "and", "agree", "agressive", "amper", "ant", "attitude"
    };

    cout << sizeof(posWords)/sizeof(string);
}